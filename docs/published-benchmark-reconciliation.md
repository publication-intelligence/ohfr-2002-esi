# Published candidate: preparation for the shared benchmark

The study's incumbent benchmark is the original shared v3 release at `98dbffd0ca171b5b7db76dbe1b2b5d5265ccacab`. Its canonical SHA-256 is `b925797fcab50b2008ad5974590e323f772e5ea7013efa84ce7606007439aeb3`; its file SHA-256 is `34a399cda8ca9f1b07b9fa0ddad36ac4f5073ef12d8b12df42fb023818508b27`.

No target benchmark has been selected. The incumbent mandate predates the native release and does not establish that v3 is better. A candidate-blind source comparison will inform the final choice among incumbent v3, native, or a reviewed v4. The inventory below describes the conditional work if a substantively different benchmark is selected, not an authorized replacement. Such a replacement would differ from the previous policy-only V8.2 migration. The preserved 89.38 result is historical, not a score-invariance requirement. PR #44 remains draft. No canonical state, benchmark, audit, calculation, report, or private archive was changed by this preparation.

The [artifact inventory](../validation/published-benchmark-reconciliation-inventory.json) records all 134 registered artifacts, their observed and registered hashes, durable-copy checks, and proposed dispositions. It is a preparation report, not a replacement evaluation state or approval to reuse judgments. The existing full private archive still passes its recorded SHA-256 and ZIP integrity check. Every registered artifact matches its durable saved-project copy. The sole registration discrepancy is the already documented source-chunk-inventory hash warning; it must remain disclosed and be resolved in a new supported context without rewriting history.

## Evidence available for reuse

| Material | Preserved extent | Required handling |
| --- | --- | --- |
| Source and mapping | Same source PDF, 425 pages, approved 17 chunks, chapter PDFs and page sidecars | Reuse unchanged bytes after validating identity and registering through the supported workflow. Reconcile the historical inventory warning. |
| Delivered candidate preparation | Layout extraction, normalized candidate, inventory and preparation QA; 1,904 paths/nodes, 5,338 locators, 16 cross-references | Retain literal candidate and stable IDs. Normalization is benchmark-independent. Validate any new registration bindings. |
| Delivered locator evidence | 17 audit chunks; all 5,338 inventory locator IDs exactly once | Preserve judgments, treatment/fit axes, evidence, confidence and uncertainty. No judgment row contains an old subject/task ID. Wrappers record the old benchmark/policy and need an explicitly supported retrospective binding, retaining original provenance. |
| Locator routing | 17 packets and routing-exception artifact | Candidate/page routing content can be reused after validation; regenerate or register packets as required by the supported command. |
| Physical cross-reference findings | Two explicit exceptions: one absent destination, one defective but identifiable destination; 16 delivered references overall | Retain evidence about physical destinations. Reassess warranted supplemental routes and all new benchmark-dependent reference obligations separately. |
| Independent structure evidence | Candidate counts, literal mechanics, delivered-heading relationship evidence, source-supported heading-fit findings and candidate-only correction evidence | Preserve source evidence; revalidate each proposed carryover. Old component rows often mix locator and benchmark evidence and cannot be copied wholesale. |

The locator rows contain 5,123 supported, 102 partially supported and 113 unsupported judgments. Their independent fit axis contains 72 `no_fit` assignments. These are preserved evidence counts, not a promised final gate count. The 13 locator-support uncertainties stay intact; a new benchmark must not silently resolve them.

## Conditional work if the benchmark changes

- If incumbent v3 is selected, rebuild ownership/worksets for its **1,366 subjects, 1,026 reader tasks and 3,210 expected treatments**. A reviewed v4 would require its own final denominators. If native is retained unchanged, these replacement worksets may not be necessary. The old 638 subject assessments, 638 task results and 1,569 treatment assessments cannot be transferred by replacing IDs or hashes. A reviewed semantic crosswalk may identify evidence that answers an equivalent requirement; every target obligation still needs a supported disposition.
- Reassess priority, acceptable access, aliases, warranted routes, relationship requirements and optional-subject applicability. The old structure ledger contains **104 explicit optional-subject decisions** tied to the superseded subjects.
- Rebuild dependent structure findings and attestations. Its 284 node exception rows contain 203 benchmark-access causal findings, 48 heading-fit findings, three subdivision findings and two cross-reference findings. These counts describe evidence to inspect, not a preapproved split between reusable and nonreusable rows. The 128 triggered architecture reviews require examination for changed access requirements.
- Preserve the 13 historical benchmark-access uncertainties, then assess target obligations and disclose whether/how their concerns map. Do not mechanically rename their targets or copy their old scope as if it covered v3.
- Review all four structured defects and their consequences. Mechanics, physical-reference and literal-heading evidence may remain applicable, while subdivision, high-priority access, denominators and dependent consequences require confirmation. Rebuild full-scope attestations and component applicability rather than inheriting a claim of target-benchmark completeness.
- Reconcile density to the study's declared common source-word measurement. The existing native measurement is 195,346 words; the legacy family uses 194,718. Candidate measurements remain evidence, but density fit must use the selected common basis.
- Regenerate calculation inputs, scores, caps, direct and other gates, assessment sufficiency, item projections, correction-overlay bindings, report and public bundle from the newly registered context. No comparative ranking or final score is assumed.

## Execution boundary

The methodology owner has been asked for the supported retrospective benchmark-migration command and schema contract. Until it is ready, preserve the current exposed state and all original freeze/review provenance. Do not run a pre-exposure legacy import against this completed state, reset `candidate_seen`, claim fresh candidate-blind discovery/review, or hand-edit bindings to bypass the workflow.

Only after the source comparison, final benchmark choice and supported migration path are ready: establish any required new context with explicit retrospective visibility; register validated source/candidate and approved locator carryover; prepare target worksets; complete necessary missing-access and structure review; rebuild and validate outputs; then verify the saved-project Git and private-artifact handoff separately. Merge, activation and publishing remain coordinated holds until the shared comparison is ready.
