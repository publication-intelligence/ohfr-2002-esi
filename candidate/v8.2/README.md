# Published Oxford index: V8.2 migration

The overall score remains **89.38/100**. Every numeric dimension component, denominator, deduction, cap evaluation and weighted contribution is identical to the frozen V8.1 result. The evaluation remains valid. Confirmed wholly wrong locator destinations and a delivered cross-reference with no valid destination now block publication directly.

The final release is methodology `c11c6ccb16000fe79646af16b7be01f6cbeeac78` (PR #50, following merged PRs #48 and #49). Two publication-gate categories trigger: **72 wholly wrong locator destinations** and **one cross-reference with no valid destination**. Publication readiness is **not publication ready**. Thirteen explicitly uncertain locator findings remain separate assessment gaps; evaluation validity remains valid.

The first `adeb691` checkpoint incorrectly propagated contextual heading-path uncertainty to sibling locators. It is preserved in Git commit `994be0d` and a verified private ZIP. The reviewed patch adds explicit scope: 13 frozen locator uncertainties affect their named destinations, while 13 benchmark-access uncertainties concern subject/task retrieval. Every original uncertainty record remains unchanged. The patch restores seven high-confidence no-fit findings to the direct gate and removes 237 path-only assessment gaps, without changing a score, cap or audit judgment.

The `individual regiments` see-also has no valid destination. The `levée en masse` reference has an identifiable destination whose delivered heading is mechanically corrupted; it retains partial-support treatment and the existing character-correction overlay. The migration adds typed destination resolution to those two frozen exceptions without changing their judgments. Newly assigned EVID identifiers map explicitly to the existing frozen findings in the migration ledger.

The policy now records an actual candidate-visible retrospective freeze, with the original candidate-blind policy and benchmark freeze retained separately. It uses the released policy builder with both the exact original V8 policy and V8.1 baseline. The old project-specific migration workaround is removed from the new policy and remains preserved in the archived baseline. Source mapping, discovery, benchmark synthesis/review/freeze, candidate normalization, all locator and missing-access audits, and all diagnostic item judgments are reused without rerun. Structure uncertainty records remain unchanged; a separately typed, evidence-bound applicability supplement identifies their scope.

The correction overlay retains all 14 confirmed headings and 18 character substitutions. Its current artifact bindings are updated, and the remaining broken reference now blocks publication after the character corrections. Original V8 and V8.1 public and private bytes remain preserved. The V8.1 canonical-state reconstruction is disclosed in `validation/v8.1-private-recovery.json`.

Validation passes for the complete public bundle, schemas, hashes, joins, public safety, canonical state, all 37 original calculation inputs, exact deterministic recalculation, numeric/cap invariance, and unchanged diagnostic item records. All 17 repository tests pass. The exact installed methodology release passes 180 methodology tests and 20 utility tests, including ten focused uncertainty-scope regressions. One pre-existing source-chunk-inventory hash warning remains; that inventory is not a calculation input.

Artifacts: `evaluation-result.v12.json`, `web-report.v10.json`, `migration-ledger.json`, `../../web/v8.2-canonical-projection/`, and `../../validation/v8.2-validation-receipt.json`. Private original/revised evidence and verified ZIP handoffs remain ignored and must accompany Git changes separately. The final private handoff is staged separately from the saved project's active V8.1 evaluation; Git activation follows PR merge. The full handoff receipt records archive and canonical-state hashes.

```sh
python scripts/validate_v8_2.py --skill /path/to/evaluate-subject-index --private
```
